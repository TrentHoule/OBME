#ifndef TYPES_H
#define TYPES_H

#include <iostream>
#include <chrono>

using Price = uint32_t; 
using Id = uint64_t;
using Quantity = uint32_t;
using TS = std::chrono::system_clock::time_point;
#endif